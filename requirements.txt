# The Vercel serverless function (api/interview.py) calls Google Gemini's REST
# API using Python's standard-library urllib, so it needs NO third-party
# packages. This file is intentionally empty of dependencies.
#
# (The legacy Streamlit/ChromaDB stack lives in requirements-streamlit.txt.)
